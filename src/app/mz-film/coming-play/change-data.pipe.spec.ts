import { ChangeDataPipe } from './change-data.pipe';

describe('ChangeDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDataPipe();
    expect(pipe).toBeTruthy();
  });
});
